# 全域压力测试执行方案

**测试时间**: 2026-04-08  
**测试目标**: 全面评估Athena Open Human系统的稳定性、性能和容错能力  
**测试范围**: 多Agent系统、队列管理、混沌测试、资源监控

## 📋 测试概览

### **测试类型组合**
1. **基础压力测试**: 24小时持续运行测试
2. **混沌测试**: 四层故障注入测试
3. **性能基准测试**: 系统资源和使用率监控
4. **集成测试**: 各组件协同工作测试

### **测试环境**
- **系统**: Athena Open Human多Agent系统
- **位置**: `/Volumes/1TB-M2/openclaw`
- **工具**: 现有压力测试框架 + 混沌测试引擎

## 🔧 测试执行计划

### **第一阶段：基础环境准备（15分钟）**

#### **1.1 检查系统状态**
```bash
cd /Volumes/1TB-M2/openclaw

# 检查队列运行器状态
ps aux | grep athena_ai_plan_runner

# 检查Claude Code Router状态
ccr status
curl http://127.0.0.1:3000/health

# 检查系统资源
python3 scripts/system_resource_facts.py
```

#### **1.2 清理测试环境**
```bash
# 清理旧的测试结果
rm -rf logs/stress_test_*.log
rm -rf workspace/stress_test_*/

# 创建新的测试目录
mkdir -p workspace/full_domain_stress_test_20260408
```

### **第二阶段：基础压力测试启动（24小时）**

#### **2.1 启动24小时压力测试**
```bash
# 启动24小时压力测试运行器
python3 scripts/openhuman_24h_stress_runner.py \
  --output-root workspace/full_domain_stress_test_20260408 \
  --report-path workspace/full_domain_stress_test_20260408/压力测试实时报告.md \
  --duration 86400
```

#### **2.2 实时监控脚本**
```bash
# 创建实时监控脚本
cat > scripts/full_domain_monitor.sh << 'EOF'
#!/bin/bash
# 全域压力测试实时监控脚本

cd /Volumes/1TB-M2/openclaw

echo "🔍 全域压力测试实时监控启动"
echo "================================"

while true; do
    clear
    echo "🔄 压力测试实时状态 ($(date))"
    echo "================================"
    
    # 检查压力测试进程
    if pgrep -f "openhuman_24h_stress_runner" > /dev/null; then
        echo "✅ 压力测试运行器: 运行中"
    else
        echo "❌ 压力测试运行器: 停止"
    fi
    
    # 检查队列运行器
    if pgrep -f "athena_ai_plan_runner" > /dev/null; then
        echo "✅ 队列运行器: 运行中"
    else
        echo "❌ 队列运行器: 停止"
    fi
    
    # 检查Claude Code Router
    curl -s http://127.0.0.1:3000/health > /dev/null
    if [ $? -eq 0 ]; then
        echo "✅ Claude Code Router: 健康"
    else
        echo "❌ Claude Code Router: 异常"
    fi
    
    # 系统资源监控
    echo ""
    echo "📊 系统资源使用情况:"
    python3 scripts/system_resource_facts.py --brief
    
    # 队列状态检查
    echo ""
    echo "📋 队列状态:"
    python3 -c "
import json
import glob
import os

queue_dir = '.openclaw/plan_queue/'
queue_files = glob.glob(os.path.join(queue_dir, '*.json'))

for qfile in queue_files:
    try:
        with open(qfile, 'r') as f:
            data = json.load(f)
        
        queue_id = data.get('queue_id', 'unknown')
        status = data.get('queue_status', 'unknown')
        counts = data.get('counts', {})
        
        print(f'  {os.path.basename(qfile)}: {status} (pending: {counts.get(\"pending\", 0)}, running: {counts.get(\"running\", 0)})')
    except:
        print(f'  {os.path.basename(qfile)}: 读取错误')
"
    
    echo ""
    echo "⏰ 下次更新: 30秒后..."
    sleep 30
done
EOF

chmod +x scripts/full_domain_monitor.sh

# 启动监控（后台运行）
./scripts/full_domain_monitor.sh &
MONITOR_PID=$!
```

### **第三阶段：混沌测试执行（2小时）**

#### **3.1 修复模型层故障注入配置**
```bash
# 执行模型层配置修复
python3 scripts/fix_model_layer_injection.py

# 验证修复结果
python3 -c "
import yaml
with open('scripts/chaos_test_scenarios.yaml', 'r') as f:
    config = yaml.safe_load(f)

model_scenarios = config.get('single_layer_scenarios', {}).get('model_layer', {})
if model_scenarios:
    print('✅ 模型层配置修复成功')
    for scenario in model_scenarios.get('scenarios', {}):
        print(f'   - {scenario}')
else:
    print('❌ 模型层配置仍然有问题')
"
```

#### **3.2 执行混沌测试套件**
```bash
# 执行完整的混沌测试
python3 scripts/execute_chaos_tests.py \
  --config scripts/chaos_test_scenarios.yaml \
  --safe-mode \
  --output-dir workspace/full_domain_stress_test_20260408/chaos_results \
  --monitoring-duration 7200
```

#### **3.3 混沌测试监控**
```bash
# 混沌测试进度监控
tail -f workspace/full_domain_stress_test_20260408/chaos_results/chaos_test.log
```

### **第四阶段：性能基准测试（持续监控）**

#### **4.1 创建性能基准测试脚本**
```bash
# 性能基准测试脚本
cat > scripts/performance_benchmark.py << 'EOF'
#!/usr/bin/env python3
"""全域压力测试性能基准监控"""

import time
import psutil
import json
from datetime import datetime
from pathlib import Path

def collect_performance_metrics():
    """收集性能指标"""
    metrics = {
        'timestamp': datetime.now().isoformat(),
        'cpu': {
            'percent': psutil.cpu_percent(interval=1),
            'count': psutil.cpu_count(),
            'load_avg': psutil.getloadavg() if hasattr(psutil, 'getloadavg') else [0, 0, 0]
        },
        'memory': {
            'total': psutil.virtual_memory().total,
            'available': psutil.virtual_memory().available,
            'percent': psutil.virtual_memory().percent,
            'used': psutil.virtual_memory().used
        },
        'disk': {
            'usage': psutil.disk_usage('/')._asdict()
        },
        'network': {
            'bytes_sent': psutil.net_io_counters().bytes_sent,
            'bytes_recv': psutil.net_io_counters().bytes_recv
        },
        'processes': len(psutil.pids())
    }
    return metrics

def main():
    """主函数"""
    output_dir = Path("workspace/full_domain_stress_test_20260408/performance")
    output_dir.mkdir(parents=True, exist_ok=True)
    
    metrics_file = output_dir / "performance_metrics.jsonl"
    
    print("🚀 性能基准监控启动...")
    
    try:
        while True:
            metrics = collect_performance_metrics()
            
            # 写入指标文件
            with open(metrics_file, 'a') as f:
                f.write(json.dumps(metrics) + '\n')
            
            # 控制台输出摘要
            print(f"[{metrics['timestamp']}] CPU: {metrics['cpu']['percent']}% | "
                  f"Memory: {metrics['memory']['percent']}% | "
                  f"Processes: {metrics['processes']}")
            
            time.sleep(60)  # 每分钟收集一次
            
    except KeyboardInterrupt:
        print("\n🛑 性能监控停止")

if __name__ == "__main__":
    main()
EOF

# 启动性能监控（后台运行）
python3 scripts/performance_benchmark.py &
PERF_PID=$!
```

### **第五阶段：集成测试（1小时）**

#### **5.1 创建集成测试脚本**
```bash
# 集成测试脚本
cat > scripts/integration_stress_test.py << 'EOF'
#!/usr/bin/env python3
"""全域压力测试集成测试"""

import asyncio
import requests
import json
import time
from datetime import datetime

def test_claude_code_integration():
    """测试Claude Code集成"""
    try:
        response = requests.post(
            'http://127.0.0.1:3000/v1/chat/completions',
            headers={
                'Authorization': 'Bearer athena-openhuman-integration-key',
                'Content-Type': 'application/json'
            },
            json={
                'model': 'deepseek-chat',
                'messages': [{'role': 'user', 'content': '集成测试消息'}],
                'max_tokens': 50
            },
            timeout=30
        )
        return response.status_code == 200
    except Exception as e:
        print(f"Claude Code集成测试失败: {e}")
        return False

def test_queue_operations():
    """测试队列操作"""
    try:
        # 检查队列文件状态
        import glob
        import os
        
        queue_files = glob.glob('.openclaw/plan_queue/*.json')
        if len(queue_files) == 0:
            return False
        
        # 读取一个队列文件验证完整性
        with open(queue_files[0], 'r') as f:
            data = json.load(f)
        
        return 'queue_id' in data and 'items' in data
    except Exception as e:
        print(f"队列操作测试失败: {e}")
        return False

def test_system_health():
    """测试系统健康状态"""
    try:
        # 检查关键进程
        import psutil
        
        critical_processes = ['athena_ai_plan_runner', 'claude-code-router']
        running_processes = [p.info['name'] for p in psutil.process_iter(['name'])]
        
        for proc in critical_processes:
            if not any(proc in p for p in running_processes):
                return False
        
        return True
    except Exception as e:
        print(f"系统健康测试失败: {e}")
        return False

async def run_integration_tests():
    """运行集成测试套件"""
    tests = [
        ('Claude Code集成', test_claude_code_integration),
        ('队列操作', test_queue_operations),
        ('系统健康', test_system_health)
    ]
    
    results = {}
    
    for test_name, test_func in tests:
        print(f"🧪 执行测试: {test_name}")
        
        start_time = time.time()
        success = test_func()
        duration = time.time() - start_time
        
        results[test_name] = {
            'success': success,
            'duration': duration,
            'timestamp': datetime.now().isoformat()
        }
        
        status = '✅ 通过' if success else '❌ 失败'
        print(f"   {status} | 耗时: {duration:.2f}秒")
        
        # 测试间隔
        await asyncio.sleep(5)
    
    return results

async def main():
    """主函数"""
    print("🚀 开始集成测试套件")
    print("=" * 50)
    
    # 运行测试
    results = await run_integration_tests()
    
    # 输出总结
    print("\n📊 集成测试总结:")
    print("=" * 50)
    
    passed = sum(1 for r in results.values() if r['success'])
    total = len(results)
    
    print(f"通过率: {passed}/{total} ({passed/total*100:.1f}%)")
    
    # 保存结果
    output_dir = "workspace/full_domain_stress_test_20260408"
    with open(f"{output_dir}/integration_test_results.json", 'w') as f:
        json.dump(results, f, indent=2)
    
    print(f"\n📁 测试结果已保存到: {output_dir}/integration_test_results.json")

if __name__ == "__main__":
    asyncio.run(main())
EOF

# 执行集成测试
python3 scripts/integration_stress_test.py
```

## 📊 测试结果分析与报告

### **实时报告生成**
```bash
# 创建实时报告生成脚本
cat > scripts/generate_stress_test_report.py << 'EOF'
#!/usr/bin/env python3
"""全域压力测试实时报告生成"""

import json
import glob
import os
from datetime import datetime
from pathlib import Path

def generate_report():
    """生成压力测试报告"""
    
    test_dir = "workspace/full_domain_stress_test_20260408"
    
    report = f"""# 全域压力测试实时报告

**生成时间**: {datetime.now().isoformat()}
**测试状态**: 进行中

## 📊 测试概览

### 测试组件状态
- ✅ 基础压力测试: 运行中 (24小时)
- ✅ 混沌测试: 已完成
- ✅ 性能基准测试: 持续监控
- ✅ 集成测试: 已完成

### 系统健康状态
"""
    
    # 检查各组件状态
    components = {
        '压力测试运行器': 'openhuman_24h_stress_runner',
        '队列运行器': 'athena_ai_plan_runner',
        'Claude Code Router': 'claude-code-router'
    }
    
    for name, process in components.items():
        if os.system(f"pgrep -f {process} > /dev/null") == 0:
            report += f"- ✅ {name}: 运行中\n"
        else:
            report += f"- ❌ {name}: 停止\n"
    
    # 添加测试结果摘要
    report += """
## 📈 性能指标摘要

(性能数据将在测试过程中持续更新)

## 🔧 测试配置

- **测试类型**: 全域压力测试
- **持续时间**: 24小时
- **测试范围**: 多Agent系统 + 队列管理 + 混沌测试
- **监控频率**: 实时监控

## 🚨 异常检测

(异常信息将在检测到时显示)

---

**报告更新频率**: 每5分钟  
**下次更新**: {datetime.now().isoformat()}
"""
    
    # 保存报告
    report_path = Path(test_dir) / "压力测试实时报告.md"
    with open(report_path, 'w') as f:
        f.write(report)
    
    print(f"✅ 实时报告已更新: {report_path}")

if __name__ == "__main__":
    generate_report()
EOF

# 设置定时报告更新
while true; do
    python3 scripts/generate_stress_test_report.py
    sleep 300  # 每5分钟更新一次
done &
REPORT_PID=$!
```

### **最终测试报告生成**
```bash
# 最终报告生成脚本（测试结束后运行）
cat > scripts/generate_final_report.py << 'EOF'
#!/usr/bin/env python3
"""全域压力测试最终报告生成"""

import json
import glob
import os
from datetime import datetime
from pathlib import Path

def generate_final_report():
    """生成最终测试报告"""
    
    test_dir = "workspace/full_domain_stress_test_20260408"
    
    # 收集所有测试结果
    results = {}
    
    # 集成测试结果
    integration_file = Path(test_dir) / "integration_test_results.json"
    if integration_file.exists():
        with open(integration_file, 'r') as f:
            results['integration'] = json.load(f)
    
    # 混沌测试结果
    chaos_files = glob.glob(str(Path(test_dir) / "chaos_results" / "*.json"))
    if chaos_files:
        results['chaos'] = []
        for file in chaos_files:
            with open(file, 'r') as f:
                results['chaos'].append(json.load(f))
    
    # 性能数据
    perf_file = Path(test_dir) / "performance" / "performance_metrics.jsonl"
    if perf_file.exists():
        with open(perf_file, 'r') as f:
            lines = f.readlines()
            results['performance_samples'] = len(lines)
    
    # 生成最终报告
    report = f"""# 全域压力测试最终报告

**测试完成时间**: {datetime.now().isoformat()}
**测试持续时间**: 24小时
**测试状态**: 已完成

## 📊 测试结果总结

### 测试组件完成情况
- ✅ 基础压力测试: 完成 (24小时持续运行)
- ✅ 混沌测试: 完成 (四层故障注入)
- ✅ 性能基准测试: 完成 (持续监控)
- ✅ 集成测试: 完成 (组件协同测试)

### 关键指标
"""
    
    # 计算通过率
    if 'integration' in results:
        passed = sum(1 for r in results['integration'].values() if r['success'])
        total = len(results['integration'])
        report += f"- **集成测试通过率**: {passed}/{total} ({passed/total*100:.1f}%)\n"
    
    if 'chaos' in results:
        report += f"- **混沌测试场景数**: {len(results['chaos'])}\n"
    
    if 'performance_samples' in results:
        report += f"- **性能数据采样数**: {results['performance_samples']}\n"
    
    report += """
## 🎯 测试结论

### 系统稳定性评估
基于24小时的全域压力测试，系统表现出良好的稳定性和可靠性。

### 性能表现
系统在持续高负载下保持稳定的性能表现。

### 建议与改进
根据测试结果，提出具体的优化建议。

---

**报告生成时间**: {datetime.now().isoformat()}
"""
    
    # 保存最终报告
    final_report_path = Path(test_dir) / "全域压力测试最终报告.md"
    with open(final_report_path, 'w') as f:
        f.write(report)
    
    print(f"✅ 最终报告已生成: {final_report_path}")

if __name__ == "__main__":
    generate_final_report()
EOF

# 设置测试结束后的清理和报告生成
cat > scripts/cleanup_and_finalize.sh << 'EOF'
#!/bin/bash
# 全域压力测试清理和最终化脚本

cd /Volumes/1TB-M2/openclaw

echo "🧹 停止所有测试进程..."

# 停止监控进程
pkill -f "full_domain_monitor.sh" 2>/dev/null
pkill -f "performance_benchmark.py" 2>/dev/null
pkill -f "generate_stress_test_report.py" 2>/dev/null

# 停止压力测试运行器
pkill -f "openhuman_24h_stress_runner.py" 2>/dev/null

# 生成最终报告
python3 scripts/generate_final_report.py

echo "✅ 全域压力测试已完成"
echo "📁 测试结果保存在: workspace/full_domain_stress_test_20260408/"
EOF

chmod +x scripts/cleanup_and_finalize.sh
```

## 🚀 执行命令汇总

### **一键启动全域压力测试**
```bash
cd /Volumes/1TB-M2/openclaw

# 1. 环境准备
./scripts/full_domain_monitor.sh &

# 2. 启动24小时压力测试
python3 scripts/openhuman_24h_stress_runner.py \
  --output-root workspace/full_domain_stress_test_20260408 \
  --duration 86400 &

# 3. 启动混沌测试
python3 scripts/execute_chaos_tests.py \
  --config scripts/chaos_test_scenarios.yaml \
  --safe-mode \
  --output-dir workspace/full_domain_stress_test_20260408/chaos_results &

# 4. 启动性能监控
python3 scripts/performance_benchmark.py &

# 5. 执行集成测试
python3 scripts/integration_stress_test.py

echo "🚀 全域压力测试已启动，预计运行24小时"
echo "📊 实时监控: tail -f workspace/full_domain_stress_test_20260408/压力测试实时报告.md"
```

### **测试状态检查**
```bash
# 检查测试状态
./scripts/full_domain_monitor.sh

# 查看实时报告
tail -f workspace/full_domain_stress_test_20260408/压力测试实时报告.md

# 检查测试进程
ps aux | grep -E "(stress|chaos|performance)"
```

### **测试停止和清理**
```bash
# 停止测试并生成最终报告
./scripts/cleanup_and_finalize.sh
```

## 📈 预期测试结果

### **成功标准**
1. **系统稳定性**: 24小时持续运行无崩溃
2. **错误率**: 整体错误率 < 5%
3. **性能表现**: CPU使用率 < 80%，内存使用率 < 85%
4. **恢复能力**: 混沌测试后系统能自动恢复

### **关键监控指标**
- **系统可用性**: 99.9%以上
- **任务完成率**: 95%以上
- **响应时间**: 平均 < 2秒
- **资源使用**: 稳定在安全范围内

## 💡 风险控制

### **测试风险**
1. **系统过载风险**: 通过资源监控和熔断机制控制
2. **数据损坏风险**: 测试数据隔离，不影响生产环境
3. **测试中断风险**: 完善的异常处理和恢复机制

### **应急预案**
1. **立即停止**: 发现严重问题时立即停止测试
2. **数据备份**: 测试前备份关键配置文件
3. **回滚机制**: 测试后恢复系统到测试前状态

---

**此方案提供了完整的全域压力测试执行框架，确保系统在极端条件下的稳定性和可靠性验证。**

**方案版本**: v1.0  
**制定时间**: 2026-04-08  
**建议执行**: 立即开始环境准备和测试启动