"""AutoResearch v0.2.0 — Karpathy workflow automation infrastructure.

Components:
    gateway/    — LLM Gateway (multi-model routing + fallback)
    codeflow/   — CodeFlow pipeline (auto-scan + trigger)
    audit_agent/— Independent audit agent (5-dimension auto-scoring)
    canary/     — Safe deployment (feature flags + canary + auto-rollback)
"""
