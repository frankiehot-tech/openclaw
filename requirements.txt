# OpenClaw 项目依赖包
# 此文件包含所有项目依赖和质量检查工具
# 注意: flake8/black/isort/pydocstyle/radon 已被 ruff 替代，不再需要

# === 核心依赖 ===
psutil>=5.9.0
requests>=2.28.0
PyYAML>=6.0
python-dotenv>=1.0.0

# === 代码质量检查工具 ===
ruff>=0.15.0
mypy>=1.0.0
coverage>=7.0.0
pytest>=7.0.0
bandit>=1.7.0
pip-audit>=2.0.0

# === 安装说明 ===
# 1. 安装所有依赖：
#    pip install -r requirements.txt
#
# 2. 仅安装核心依赖：
#    pip install psutil requests PyYAML python-dotenv
#
# 3. 仅安装质量检查工具：
#    pip install ruff mypy coverage pytest bandit pip-audit
